import * as React from "react"
import { Container, Typography } from "@mui/material"

const Footer = () => (
  <Container maxWidth="lg" sx={{p: "20px"}}>
    <Typography variant="p" color="initial">
      ©2023 «Electronics store»
    </Typography>
  </Container>
)

export default Footer
